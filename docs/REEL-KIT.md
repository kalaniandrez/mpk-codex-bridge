# MPK Codex Bridge reel kit

## Reel concept

**Hook:** I turned this music keyboard into a controller for Codex.

Target length: 18 to 24 seconds. Record vertically at 1080 × 1920. Keep the MPK
in the lower third and the Mac screen readable above it.

## Shot list and voiceover

| Time | Picture | Voiceover / on-screen text |
| --- | --- | --- |
| 0:00-0:03 | Tight shot: hand hits a pad, Codex jumps forward | “I turned my Akai keyboard into a controller for Codex.” |
| 0:03-0:07 | Show Last MIDI changing while two pads control Codex | “Each pad can open a task, confirm, cancel, or start dictation.” |
| 0:07-0:11 | Turn two knobs, then show their mapped actions | “And the synth knobs now navigate the app.” |
| 0:11-0:15 | Show the Learn button and one fast mapping | “It learns your MIDI controls locally, with no API key.” |
| 0:15-0:19 | Split shot: MPK plus a real Codex action | “I made the Mac app open source.” |
| 0:19-0:23 | Cover card | “Comment MIDI and I’ll send you the download.” |

Keep the classic app open beside Codex. Frame the **Last MIDI** and **Last
action** card when demonstrating input, then scroll to the mapped controls.

## On-screen caption stack

1. `MUSIC KEYBOARD → CODEX CONTROLLER`
2. `8 PADS · 4 KNOBS · NO API KEY`
3. `LOCAL-ONLY + OPEN SOURCE`
4. `COMMENT “MIDI” FOR THE MAC DOWNLOAD`

Keep every caption inside the central 80 percent of the frame so Instagram's
controls do not cover it.

## Cover

Use `docs/media/reel-cover-background.png` and add:

**Headline:** I TURNED MY MPK INTO A CODEX CONTROLLER

**Small line:** Free Mac app

Keep the headline in the upper-middle area and leave the controller illustration
visible.

## ManyChat trigger

Keyword: `MIDI`

Public download:

`https://github.com/kalaniandrez/mpk-codex-bridge/releases/latest/download/MPK-Codex-Bridge-macOS.zip`

### First automated reply

> Here it is 🎹 MPK Codex Bridge turns your MIDI pads and knobs into shortcuts
> for the Codex Mac app. Download it here:
> {{download_link}}
>
> macOS 13+ · local-only · no API key. On first launch, Control-click the app
> and choose Open.

### Follow-up reply

> Quick knob fix: turn Internal Sounds off and learn the Filter, Resonance,
> Reverb, or Chorus knob. The separate Volume knob does not send assignable
> MIDI. Full setup:
> https://github.com/kalaniandrez/mpk-codex-bridge/blob/main/docs/INSTALL.md

### Test checklist

- Trigger matching is case-insensitive for `MIDI`.
- The button opens the stable `releases/latest/download` URL.
- The ZIP downloads without a GitHub login.
- The first reply says Mac and macOS 13+ before the click.
- The follow-up explains the Volume/Internal Sounds issue.

## Suggested caption

I turned an Akai MPK Mini Play into a physical controller for Codex. The pads
can open tasks, confirm, cancel, navigate, and trigger dictation. The four
assignable knobs can navigate too.

The bridge is local-only, open source, and needs no API key.

Comment **MIDI** and I’ll DM you the Mac download.

Independent community project. Not affiliated with OpenAI or Akai.
